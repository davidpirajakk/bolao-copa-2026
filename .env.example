# Porta do servidor (Railway/Render definem automaticamente)
PORT=3000

# Caminho do banco de dados SQLite (opcional, padrão: ./bolao.db)
# No Railway: monte um volume e aponte para /data/bolao.db
DB_PATH=./bolao.db
